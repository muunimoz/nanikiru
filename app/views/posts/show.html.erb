<h1>詳細</h1>

  <%= image_tag @post.get_image %>
  <p>ユーザーネーム：<%= @post.user.name %></p>
  <p>地域：<%= @post.area_name %></p>
  <p>気温：<%= @post.temperature_name %></p>
  <p>説明：<%= @post.text %></p>
  <p>投稿日：<%= @post.created_at.strftime('%Y/%m/%d') %></p>
  
  <% if @post.user == current_user %>
    <%= link_to "編集", edit_post_path(@post.id) %>
  <% end %>
  
  <% if @post.favorited_by?(current_user) %>
    <p>
      <%= link_to post_favorite_path(@post), method: :delete do %>
        ♥<%= @post.favorites.count %> いいね
      <% end %>
    </p>
    <% else %>
    <p>
      <%= link_to post_favorite_path(@post), method: :post do %>
        ♡<%= @post.favorites.count %> いいね
      <% end %>
    </p>
  <% end %>
  
  <% @favorite_users.each do |user| %>
  <div>
    <%= link_to image_tag(@user.get_profile_image(100,100)), user_path(user.id) %>
  </div>
  <% end %>

<div>
  <p>コメント件数：<%= @post.comments.count %></p>
  <% @post.comments.each do |comments| %>
    <p><%= image_tag comments.user.get_profile_image(100,100) %></p>
    <%= comments.user.name %>
    <%= comments.created_at.strftime('%Y/%m/%d') %><%= comments.comment %>
    <% if comments.user == current_user %>
      <%= link_to "削除", post_comment_path(comments.post, comments), method: :delete %>
    <% end %>
  <% end %>
</div>
<div>
  <%= form_with model: [@post, @comment] do |f| %>
    <%= f.text_area :comment, placeholder: "コメントをここに" %>
    <%= f.submit "送信する" %>
  <% end %>
</div>