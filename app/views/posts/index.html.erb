<h1>ALL</h1>
<%=link_to "新規投稿する", new_post_path %>

<% @posts.each do |post| %>
  <div>
    <%= image_tag post.get_image %>
    <%#= link_to image_tag post.get_image, post_path(post.id) %>
    <%= link_to "詳細", post_path(post.id) %>
  </div>
<% end %>