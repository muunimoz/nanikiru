<h1>ユーザー一覧</h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.id, edit_admin_user_path(user.id) %> - <%= image_tag user.get_profile_image(100, 100) %> - <%= user.name %> - <%= user.email %> - <%= user.is_active %> - <%= user.created_at %>
    </li>
  <% end %>
</ul>